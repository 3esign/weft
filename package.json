{
  "name": "weft",
  "private": true,
  "description": "WEFT continuous lattice toolpath studio - the app itself is buildless (open index.html; the engine is core/weft_core.js and the gate core/weft_gate.js, shared with `node weft.mjs`); this package exists only for the headless test harness.",
  "type": "module",
  "scripts": {
    "test": "node tests/run_all.mjs",
    "test:legacy": "node tests/run_tests.mjs",
    "test:mcp": "node tests/mcp.test.mjs",
    "test:limit16": "node tests/limit16.test.mjs",
    "test:parity": "node tests/build_parity.test.mjs && node tests/gate_parity.test.mjs && node tests/core_parity.test.mjs",
    "mcp": "node mcp/server.mjs",
    "build": "node weft.mjs build",
    "check": "node weft.mjs check",
    "serve": "node weft.mjs serve"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
