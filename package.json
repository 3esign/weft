{
  "name": "weft",
  "private": true,
  "description": "WEFT continuous lattice toolpath studio - the app itself is buildless (open index.html); this package exists only for the headless test harness.",
  "type": "module",
  "scripts": {
    "test": "node tests/run_tests.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
