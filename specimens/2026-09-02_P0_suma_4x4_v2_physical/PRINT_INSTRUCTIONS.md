# P0 Šuma 4×4 v2 — print instructions

Route B, ~4.7 h kinematic (Bambu will say more). Send `P0_suma_4x4_v2_routeB.gcode` to the A2L.
White PLA, textured PEI plate, same spool as the 2×2.

Watch the **first layer** — it is now 232 brim paths with hops between them instead of one slab. If beads lift or
string badly, stop and tell me; that is the one part that changed most.

Photograph: the brim web before the columns rise, z ≈ 13 (first merge), z ≈ 25 (second merge + first hole caps),
z ≈ 34 and 38 (last merges), the finish, and then the **hole caps from below** — those are the whole point of this
version, and whether they are clean membranes or tangles decides whether the same rule fixes the D5 dome crown.
